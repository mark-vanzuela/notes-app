# Keep host build output out of the image build context, otherwise it clobbers
# the container's restore and breaks `dotnet publish --no-restore`.
**/bin/
**/obj/
**/*.user
.vs/
Dockerfile
.dockerignore
README.md
