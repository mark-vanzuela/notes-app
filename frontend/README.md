# Frontend — React + Vite + TypeScript

React single-page app for the Notes App. Talks to the API over HTTP/JSON.

> **Status:** placeholder. The Vite + React + TS app is scaffolded in its own
> planning round. Expected here once built:
>
> - `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`
> - `src/` — components, API client, routing
> - `Dockerfile` — multi-stage build → static serve (nginx)
> - `.env` via `VITE_API_BASE_URL` (see root `.env.example`)
