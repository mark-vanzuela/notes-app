// Runs once before each test file (configured as `setupFiles` in vite.config.ts).
// Importing jest-dom adds friendly DOM matchers to `expect`, e.g.
//   expect(element).toBeInTheDocument()
//   expect(button).toBeDisabled()
import '@testing-library/jest-dom';
