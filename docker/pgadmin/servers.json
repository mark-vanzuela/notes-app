{
  "Servers": {
    "1": {
      "Name": "Notes (local)",
      "Group": "Servers",
      "Host": "db",
      "Port": 5432,
      "MaintenanceDB": "notesdb",
      "Username": "notes",
      "SSLMode": "prefer",
      "Comment": "Local Compose Postgres. Password = POSTGRES_PASSWORD (default 'notes_dev_password')."
    }
  }
}
